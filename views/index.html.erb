<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Zed Thread Viewer</title>
  <link rel="stylesheet" href="<%= asset_with_hash('index.css') %>">
</head>
<body>
  <div class="container">
    <div class="titles-section">
      <div class="search-controls">
        <input type="text" id="search" placeholder="Search... (/)" oninput="searchTitles()">
        <button id="star-filter-btn" onclick="toggleStarFilter()" title="Filter starred" tabindex="-1">☆</button>
        <button id="reload-btn" onclick="runImport()" title="Reload data" tabindex="-1">⟳</button>
        <button id="layout-btn" onclick="toggleLayout()" title="Toggle layout" tabindex="-1">⚏</button>
      </div>
      <div class="table-container" tabindex="0">
        <div class="titles-table" id="titles">
          <div class="grid-header col-star"></div>
          <div class="grid-header col-symbol"></div>
          <div class="grid-header col-date">Date</div>
          <div class="grid-header col-title">Title</div>
          <div class="grid-header col-project">Project</div>
          <!-- Rows loaded via JavaScript -->
        </div>
      </div>
    </div>
    <div class="content-section">
      <div class="content" id="content-container">
        <!-- Iframes will be dynamically added here -->
      </div>
    </div>
  </div>

  <div class="status-bar">
    <span class="shortcut"><kbd>/</kbd> search</span>
    <span class="shortcut"><kbd>↑↓</kbd> navigate</span>
    <span class="shortcut"><kbd>c</kbd> copy title</span>
    <span class="shortcut"><kbd>f</kbd> star</span>
    <span class="shortcut"><kbd>j</kbd> toggle json/text</span>
    <span class="shortcut"><kbd>v</kbd> toggle layout</span>
    <span class="shortcut"><kbd>r</kbd> rebuild database</span>
  </div>

  <script src="<%= asset_with_hash('index.js') %>"></script>
</body>
</html>
