<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= CGI.escapeHTML(title) %></title>
  <link rel="stylesheet" href="<%= asset_with_hash('shared.css') %>">
  <style>
    .slash-command {
      background: #1C212B;
      border: 1px solid #1A1F29;
      border-radius: 6px;
      padding: 2px 6px;
      font-family: 'SF Mono', Monaco, 'Menlo', monospace;
      font-size: 9px;
      color: #FFCC66;
      font-weight: 600;
      display: inline-block;
      margin: 2px 0;
    }
    .role-label {
      background: #1C212B;
      border: 1px solid #1A1F29;
      border-radius: 6px;
      padding: 2px 8px;
      font-family: 'SF Mono', Monaco, 'Menlo', monospace;
      font-size: 9px;
      color: #73D0FF;
      font-weight: 600;
      display: block;
      margin: 12px 0 4px 0;
      text-align: center;
      width: fit-content;
    }
    h1 { color: #5CCFE6; margin: 16px 0 12px 0; font-size: 14px; }
    h2 { color: #FFD173; margin: 14px 0 8px 0; font-size: 12px; }
    h3 { color: #FFAD66; margin: 12px 0 6px 0; font-size: 11px; }
    p { margin: 8px 0; }
    pre {
      background: #1C212B;
      padding: 12px;
      border-radius: 4px;
      border: 1px solid #1A1F29;
      font-family: 'SF Mono', Monaco, 'Menlo', monospace;
      font-size: 9px;
      margin: 12px 0;
      overflow-x: auto;
    }
    .highlight {
      background: #1C212B;
      border-radius: 4px;
    }
    .highlight pre {
      margin: 0;
      background: none;
    }
    .highlight .k { color: #FFAD66; } /* keyword */
    .highlight .s { color: #D5FF80; } /* string */
    .highlight .mi { color: #DFBFFF; } /* number */
    .highlight .c1, .highlight .c { color: rgba(184, 207, 230, 0.5); } /* comment */
    .highlight .nf { color: #FFD173; } /* function */
    .highlight .nb { color: #73D0FF; } /* builtin */
    code {
      background: #1C212B;
      padding: 1px 2px;
      border-radius: 2px;
      border: 1px solid #1A1F29;
      font-family: Monaco, Consolas, monospace;
      font-size: 9px;
      color: #FFDFB3;
    }
    pre code {
      background: none;
      padding: 0;
    }
    strong { font-weight: bold; }
    em { font-style: italic; }
  </style>
</head>
<body>
  <%= render("_header.html", locals: { title: title, type: type, id: id, toggle_url: toggle_url, toggle_text: toggle_text, starred: starred }) %>
  <div><%= html_content %></div>


</body>
</html>
